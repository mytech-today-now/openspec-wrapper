# npm registry configuration for @mytechtoday scoped packages
# Published to the public npmjs.com registry.
# Run `npm login` before publishing:
#   npm login   (authenticates to registry.npmjs.org)
@mytechtoday:registry=https://registry.npmjs.org

