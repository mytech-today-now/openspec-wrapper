Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 1):
bd-3brp [task] P2 [open] - [wrapper][P1] T-1.1 Add ESM module type to package.json
bd-1kc1 [task] P2 [open] - [wrapper][P1] T-1.2 Install runtime dependencies
bd-c9x6 [task] P2 [open] - [wrapper][P1] T-1.3 Install dev dependencies
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 2):
bd-t9c7 [task] P3 [open] - [wrapper][P1] T-1.6 Add .env files to .gitignore
bd-fst3 [task] P1 [open] - [wrapper][P2] T-2.3 Add API key startup validation
bd-bvzv [task] P1 [open] - [wrapper][P3] T-3.3 Define ExecuteOpenspecSchema with Zod
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 3):
bd-xs2x [task] P1 [open] - [wrapper][P4] T-4.1 Define system prompt constant
bd-o7me [task] P1 [open] - [wrapper][P5] T-5.6 Open PR with end-to-end recording
bd-2gos [task] P1 [open] - [wrapper][P4] T-4.5 Implement main entry point
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 4):
bd-jhsv [task] P1 [open] - [wrapper][P3] T-3.8 Implement dispatchTool function
bd-ahr1 [task] P1 [open] - [wrapper][P3] T-3.5 Implement executeOpenspec function
bd-1uu0 [task] P1 [open] - [wrapper][P4] T-4.3 Implement runAgent core ReAct loop
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 5):
bd-66f8 [task] P1 [open] - [wrapper][P5] T-5.2 End-to-end test: verify propose precedes apply
bd-w7i8 [task] P1 [open] - [wrapper][P5] T-5.1 Run tsc --noEmit --strict and fix all type errors
bd-qqrh [task] P2 [open] - [wrapper][P2] T-2.1 Define AgentConfig TypeScript interface
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 6):
bd-re1e [task] P2 [open] - [wrapper][P3] T-3.7 Register execute_openspec in registry
bd-d1ec [task] P2 [open] - [wrapper][P3] T-3.2 Instantiate tool registry Map
bd-43cy [task] P2 [open] - [wrapper][P4] T-4.2 Implement buildToolSchema function
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 7):
bd-gr0s [task] P2 [open] - [wrapper][P1] T-1.5 Create tsconfig.json
bd-b2yg [task] P2 [open] - [wrapper][P3] T-3.4 Implement shellQuote helper function
bd-ceec [task] P2 [open] - [wrapper][P5] T-5.5 Update root README with AI Development Tools section
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 8):
bd-nkvu [task] P2 [open] - [wrapper][P3] T-3.6 Implement stderr scrubbing
bd-evku [task] P2 [open] - [wrapper][P5] T-5.4 Debug mode test: payloads printed to stderr
bd-xsdu [task] P2 [open] - [wrapper][P1] T-1.4 Add npm scripts to package.json
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 9):
bd-c4ud [task] P2 [open] - [wrapper][P5] T-5.3 Failure test: invalid or missing API key
bd-1xxf [task] P2 [open] - [wrapper][P2] T-2.4 Add openspec CLI availability check
bd-ie5t [task] P2 [open] - [wrapper][P2] T-2.2 Implement resolveConfig function
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 10):
bd-na5t [task] P2 [open] - [wrapper][P3] T-3.1 Define ToolDefinition generic interface
bd-mbt2 [task] P3 [open] - [wrapper][P4] T-4.6 Apply consistent emoji prefix format to all output
bd-8eft [task] P3 [open] - [wrapper][P2] T-2.5 Smoke-test ai-powered client wiring
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'

---

::::: START HERE :::::

Using Augmentcode AI (with Augment-extensions) in VS Code:
- Load bead tasks using 'scripts\beads-helpers.ps1' (dot-sourceit to get the 'bd' alias: . .\scripts\beads-helpers.ps1) or call 'scripts\beads-query.ps1' directly
- Check task completion status with "bd list --status open" or "bd ready " — skip any task whose status is not open/in-progress
- For each remaining task in this batch:
    - Claim the task before starting: "bd update <id> --claim"
    - Generate production-quality code that fully satisfies the bead task requirements
    - Follow professional coding standards at all times
    - Do not use stubs, placeholders, or incomplete implementations
    - Do not hallucinate or make up functionality
    - Never reuse the same code pattern for multiple distinct tasks
    - Address every TODO in the relevant files:
        • If a TODO is relevant, implement the required change
        • If a TODO is not relevant, explicitly document why it can be ignored
    - Do not proceed until all TODOs are explicitly resolved or justified
(batch 11):
bd-ypnq [task] P3 [open] - [wrapper][P4] T-4.4 Add DEBUG logging to ReAct loop
bd-h5g6 [task] P3 [open] - [wrapper][P3] T-3.9 Manual integration test for executeOpenspec list
After completing the tasks above:
- Mark the processed bead task(s) as closed in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl'.  Do NOT delete the bead task from 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\.beads\issues.jsonl' — only mark it as closed.
- Also record completion in 'G:\_kyle\temp_documents\GitHub\openspec-wrapper\completed.jsonl'
